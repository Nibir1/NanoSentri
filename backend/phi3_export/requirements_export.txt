# CRITICAL: Pin NumPy to prevent the "2.x" crash
numpy<2.0

# Core Stack (Exact versions known to work together)
transformers==4.41.2
peft==0.11.1
accelerate==0.30.1

# Optimum 1.20.0 (Required for Phi-3 support)
optimum[onnxruntime]==1.20.0

# Utilities
protobuf==3.20.3
scipy